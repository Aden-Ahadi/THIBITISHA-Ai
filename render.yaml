services:
  - type: web
    name: thibitisha-ai-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
    disk:
      name: ai-bot-disk
      mountPath: /tmp
      sizeGB: 1